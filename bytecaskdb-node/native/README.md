# Native N-API Backend (Planned)

This directory will contain the node-addon-api (N-API) binding for ByteCaskDB,
providing a native Node.js addon as an alternative to the WASM backend.

Not yet implemented. See the `wasm/` directory for the current implementation.
