// SPDX-License-Identifier: GPL-2.0-only
// Copyright (c) 2026 Gustavo Amigo
//
// prove_plugin.cpp — AUTO-GENERATED by tests/proof/generate_tests.py
// DO NOT EDIT BY HAND. Regenerate with:
//   cd bytecaskdb-mariadb-plugin && python3 -m tests.proof.generate_tests

#include "harness.h"
#include "fault_injector.h"

#include <catch2/catch_test_macros.hpp>

using namespace bytecaskdb::testing;
using namespace bytecask::testing;

TEST_CASE("prove_plugin__single_insert__pk_only__autocommit__SUCCESS", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;

  PluginTestHarness h(spec);

  int rc = h.insert_row({100, 200, 300});

  REQUIRE(rc == 0);
  REQUIRE(h.row_counter() == 1);
}

TEST_CASE("prove_plugin__single_insert__pk_only__autocommit__ENGINE_DEGRADED", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;

  PluginTestHarness h(spec);

  ScopedFaultInjector guard("io_data_file_append");

  int rc = h.insert_row({100, 200, 300});

  int commit_rc = h.commit();

  // DML buffers successfully; fault fires at commit.
  REQUIRE(rc == 0);
  REQUIRE(commit_rc != 0);
}

TEST_CASE("prove_plugin__single_insert__one_nonunique__autocommit__SUCCESS", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;
  spec.secondary_indexes.push_back({1, 1, false, {1}});

  PluginTestHarness h(spec);

  int rc = h.insert_row({100, 200, 300});

  REQUIRE(rc == 0);
  REQUIRE(h.row_counter() == 1);
}

TEST_CASE("prove_plugin__single_insert__one_nonunique__autocommit__ENGINE_DEGRADED", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;
  spec.secondary_indexes.push_back({1, 1, false, {1}});

  PluginTestHarness h(spec);

  ScopedFaultInjector guard("io_data_file_append");

  int rc = h.insert_row({100, 200, 300});

  int commit_rc = h.commit();

  // DML buffers successfully; fault fires at commit.
  REQUIRE(rc == 0);
  REQUIRE(commit_rc != 0);
}

TEST_CASE("prove_plugin__single_insert__one_unique__autocommit__SUCCESS", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;
  spec.secondary_indexes.push_back({1, 1, true, {1}});

  PluginTestHarness h(spec);

  int rc = h.insert_row({100, 200, 300});

  REQUIRE(rc == 0);
  REQUIRE(h.row_counter() == 1);
}

TEST_CASE("prove_plugin__single_insert__one_unique__autocommit__ENGINE_DEGRADED", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;
  spec.secondary_indexes.push_back({1, 1, true, {1}});

  PluginTestHarness h(spec);

  ScopedFaultInjector guard("io_data_file_append");

  int rc = h.insert_row({100, 200, 300});

  int commit_rc = h.commit();

  // DML buffers successfully; fault fires at commit.
  REQUIRE(rc == 0);
  REQUIRE(commit_rc != 0);
}

TEST_CASE("prove_plugin__single_delete__pk_only__autocommit__SUCCESS", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;

  PluginTestHarness h(spec);

  // Pre-insert a row to operate on.
REQUIRE(h.insert_row({10, 20, 30}) == 0);
REQUIRE(h.commit() == 0);

  int rc = h.delete_row({10, 20, 30});

  REQUIRE(rc == 0);
  // Row counter decremented after delete.
  REQUIRE(h.row_counter() == 0);
}

TEST_CASE("prove_plugin__single_delete__pk_only__autocommit__OCC_CONFLICT", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;

  PluginTestHarness h(spec);

  // Pre-insert a row to operate on.
REQUIRE(h.insert_row({10, 20, 30}) == 0);
REQUIRE(h.commit() == 0);

  int rc = h.delete_row({10, 20, 30});

  // OCC conflict — commit should fail.
  REQUIRE(rc == 0);
}

TEST_CASE("prove_plugin__single_delete__pk_only__autocommit__ENGINE_DEGRADED", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;

  PluginTestHarness h(spec);

  // Pre-insert a row to operate on.
REQUIRE(h.insert_row({10, 20, 30}) == 0);
REQUIRE(h.commit() == 0);

  ScopedFaultInjector guard("io_data_file_append");

  int rc = h.delete_row({10, 20, 30});

  int commit_rc = h.commit();

  // DML buffers successfully; fault fires at commit.
  REQUIRE(rc == 0);
  REQUIRE(commit_rc != 0);
}

TEST_CASE("prove_plugin__single_delete__one_nonunique__autocommit__SUCCESS", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;
  spec.secondary_indexes.push_back({1, 1, false, {1}});

  PluginTestHarness h(spec);

  // Pre-insert a row to operate on.
REQUIRE(h.insert_row({10, 20, 30}) == 0);
REQUIRE(h.commit() == 0);

  int rc = h.delete_row({10, 20, 30});

  REQUIRE(rc == 0);
  // Row counter decremented after delete.
  REQUIRE(h.row_counter() == 0);
}

TEST_CASE("prove_plugin__single_delete__one_nonunique__autocommit__OCC_CONFLICT", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;
  spec.secondary_indexes.push_back({1, 1, false, {1}});

  PluginTestHarness h(spec);

  // Pre-insert a row to operate on.
REQUIRE(h.insert_row({10, 20, 30}) == 0);
REQUIRE(h.commit() == 0);

  int rc = h.delete_row({10, 20, 30});

  // OCC conflict — commit should fail.
  REQUIRE(rc == 0);
}

TEST_CASE("prove_plugin__single_delete__one_nonunique__autocommit__ENGINE_DEGRADED", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;
  spec.secondary_indexes.push_back({1, 1, false, {1}});

  PluginTestHarness h(spec);

  // Pre-insert a row to operate on.
REQUIRE(h.insert_row({10, 20, 30}) == 0);
REQUIRE(h.commit() == 0);

  ScopedFaultInjector guard("io_data_file_append");

  int rc = h.delete_row({10, 20, 30});

  int commit_rc = h.commit();

  // DML buffers successfully; fault fires at commit.
  REQUIRE(rc == 0);
  REQUIRE(commit_rc != 0);
}

TEST_CASE("prove_plugin__single_delete__one_unique__autocommit__SUCCESS", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;
  spec.secondary_indexes.push_back({1, 1, true, {1}});

  PluginTestHarness h(spec);

  // Pre-insert a row to operate on.
REQUIRE(h.insert_row({10, 20, 30}) == 0);
REQUIRE(h.commit() == 0);

  int rc = h.delete_row({10, 20, 30});

  REQUIRE(rc == 0);
  // Row counter decremented after delete.
  REQUIRE(h.row_counter() == 0);
}

TEST_CASE("prove_plugin__single_delete__one_unique__autocommit__OCC_CONFLICT", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;
  spec.secondary_indexes.push_back({1, 1, true, {1}});

  PluginTestHarness h(spec);

  // Pre-insert a row to operate on.
REQUIRE(h.insert_row({10, 20, 30}) == 0);
REQUIRE(h.commit() == 0);

  int rc = h.delete_row({10, 20, 30});

  // OCC conflict — commit should fail.
  REQUIRE(rc == 0);
}

TEST_CASE("prove_plugin__single_delete__one_unique__autocommit__ENGINE_DEGRADED", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;
  spec.secondary_indexes.push_back({1, 1, true, {1}});

  PluginTestHarness h(spec);

  // Pre-insert a row to operate on.
REQUIRE(h.insert_row({10, 20, 30}) == 0);
REQUIRE(h.commit() == 0);

  ScopedFaultInjector guard("io_data_file_append");

  int rc = h.delete_row({10, 20, 30});

  int commit_rc = h.commit();

  // DML buffers successfully; fault fires at commit.
  REQUIRE(rc == 0);
  REQUIRE(commit_rc != 0);
}

TEST_CASE("prove_plugin__single_update_index_change__pk_only__autocommit__SUCCESS", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;

  PluginTestHarness h(spec);

  // Pre-insert a row to operate on.
REQUIRE(h.insert_row({10, 20, 30}) == 0);
REQUIRE(h.commit() == 0);

  int rc = h.update_row({10, 20, 30}, {10, 99, 30});

  REQUIRE(rc == 0);
  // Update does not change row count.
  REQUIRE(h.row_counter() == 1);
}

TEST_CASE("prove_plugin__single_update_index_change__pk_only__autocommit__OCC_CONFLICT", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;

  PluginTestHarness h(spec);

  // Pre-insert a row to operate on.
REQUIRE(h.insert_row({10, 20, 30}) == 0);
REQUIRE(h.commit() == 0);

  int rc = h.update_row({10, 20, 30}, {10, 99, 30});

  // OCC conflict — commit should fail.
  REQUIRE(rc == 0);
}

TEST_CASE("prove_plugin__single_update_index_change__pk_only__autocommit__ENGINE_DEGRADED", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;

  PluginTestHarness h(spec);

  // Pre-insert a row to operate on.
REQUIRE(h.insert_row({10, 20, 30}) == 0);
REQUIRE(h.commit() == 0);

  ScopedFaultInjector guard("io_data_file_append");

  int rc = h.update_row({10, 20, 30}, {10, 99, 30});

  int commit_rc = h.commit();

  // DML buffers successfully; fault fires at commit.
  REQUIRE(rc == 0);
  REQUIRE(commit_rc != 0);
}

TEST_CASE("prove_plugin__single_update_index_change__one_nonunique__autocommit__SUCCESS", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;
  spec.secondary_indexes.push_back({1, 1, false, {1}});

  PluginTestHarness h(spec);

  // Pre-insert a row to operate on.
REQUIRE(h.insert_row({10, 20, 30}) == 0);
REQUIRE(h.commit() == 0);

  int rc = h.update_row({10, 20, 30}, {10, 99, 30});

  REQUIRE(rc == 0);
  // Update does not change row count.
  REQUIRE(h.row_counter() == 1);
}

TEST_CASE("prove_plugin__single_update_index_change__one_nonunique__autocommit__OCC_CONFLICT", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;
  spec.secondary_indexes.push_back({1, 1, false, {1}});

  PluginTestHarness h(spec);

  // Pre-insert a row to operate on.
REQUIRE(h.insert_row({10, 20, 30}) == 0);
REQUIRE(h.commit() == 0);

  int rc = h.update_row({10, 20, 30}, {10, 99, 30});

  // OCC conflict — commit should fail.
  REQUIRE(rc == 0);
}

TEST_CASE("prove_plugin__single_update_index_change__one_nonunique__autocommit__ENGINE_DEGRADED", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;
  spec.secondary_indexes.push_back({1, 1, false, {1}});

  PluginTestHarness h(spec);

  // Pre-insert a row to operate on.
REQUIRE(h.insert_row({10, 20, 30}) == 0);
REQUIRE(h.commit() == 0);

  ScopedFaultInjector guard("io_data_file_append");

  int rc = h.update_row({10, 20, 30}, {10, 99, 30});

  int commit_rc = h.commit();

  // DML buffers successfully; fault fires at commit.
  REQUIRE(rc == 0);
  REQUIRE(commit_rc != 0);
}

TEST_CASE("prove_plugin__single_update_index_change__one_unique__autocommit__SUCCESS", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;
  spec.secondary_indexes.push_back({1, 1, true, {1}});

  PluginTestHarness h(spec);

  // Pre-insert a row to operate on.
REQUIRE(h.insert_row({10, 20, 30}) == 0);
REQUIRE(h.commit() == 0);

  int rc = h.update_row({10, 20, 30}, {10, 99, 30});

  REQUIRE(rc == 0);
  // Update does not change row count.
  REQUIRE(h.row_counter() == 1);
}

TEST_CASE("prove_plugin__single_update_index_change__one_unique__autocommit__OCC_CONFLICT", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;
  spec.secondary_indexes.push_back({1, 1, true, {1}});

  PluginTestHarness h(spec);

  // Pre-insert a row to operate on.
REQUIRE(h.insert_row({10, 20, 30}) == 0);
REQUIRE(h.commit() == 0);

  int rc = h.update_row({10, 20, 30}, {10, 99, 30});

  // OCC conflict — commit should fail.
  REQUIRE(rc == 0);
}

TEST_CASE("prove_plugin__single_update_index_change__one_unique__autocommit__ENGINE_DEGRADED", "[prove_plugin]") {
  TableSpec spec;
  spec.num_int_columns = 3;
  spec.has_pk = true;
  spec.secondary_indexes.push_back({1, 1, true, {1}});

  PluginTestHarness h(spec);

  // Pre-insert a row to operate on.
REQUIRE(h.insert_row({10, 20, 30}) == 0);
REQUIRE(h.commit() == 0);

  ScopedFaultInjector guard("io_data_file_append");

  int rc = h.update_row({10, 20, 30}, {10, 99, 30});

  int commit_rc = h.commit();

  // DML buffers successfully; fault fires at commit.
  REQUIRE(rc == 0);
  REQUIRE(commit_rc != 0);
}
