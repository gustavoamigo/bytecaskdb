FROM docker.io/library/ubuntu:22.04
RUN apt-get update && apt-get install -y --no-install-recommends \
    sysbench mariadb-client bc && \
    rm -rf /var/lib/apt/lists/*
ENTRYPOINT ["/benchmark/run-benchmark.sh"]
